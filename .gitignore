.DS_Store
Thumbs.db
.claude/
VAPID-KEYS-PRIVATE.txt

# ffmpeg is downloaded, not written here — macro-studio finds it on PATH,
# and the build is 650MB with three files over GitHub's 100MB limit
ffmpeg-*-full_build/
