.DS_Store
Thumbs.db
.claude/
VAPID-KEYS-PRIVATE.txt

# ffmpeg is downloaded, not written here — macro-studio finds it on PATH,
# and the build is 650MB with three files over GitHub's 100MB limit
ffmpeg-*-full_build/

# Macro Studio's own settings on each PC — they hold a Supabase login, so
# they belong on that machine and nowhere else.
macro-studio/duoke.json
macro-studio/labels.json

# the Supabase CLI's own scratch directory for this machine
supabase/.temp/
